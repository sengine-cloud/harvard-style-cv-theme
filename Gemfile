source "https://rubygems.org"

# This theme is packaged as a gem (see harvard-style-cv-theme.gemspec).
# `gemspec` pulls in its runtime dependencies (jekyll, jekyll-seo-tag) so the
# bundled demo site in this repo can be built/served locally.
gemspec

# webrick is no longer a default gem on Ruby 3+, needed for `jekyll serve`.
gem "webrick", "~> 1.8"
